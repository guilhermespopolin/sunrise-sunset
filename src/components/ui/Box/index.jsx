import styled from 'styled-components'
import {
  layout,
  flexbox,
  grid,
  space,
  color,
  typography,
  background,
  border,
  shadow,
  position,
} from 'styled-system'

const Box = styled.div`
  ${layout}
  ${flexbox}
  ${grid}
  ${space}
  ${typography}
  ${color}
  ${background}
  ${border}
  ${shadow}
  ${position}
`

export default Box
